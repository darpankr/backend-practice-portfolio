
server:
  port: 8080

logging:
  level:
    com.example.darpan.portfolio_practice: DEBUG
    ORG.springframework.web: INFO
    org.hibernate.SQL: DEBUG

cors:
  allowed-origin:
    - http://locahost:8080
    - http://localhost:5173

spring:
  application:
    name: portfolio-practice

  datasource:
    url: jdbc:mysql://localhost:3306/portfolio_db?useSSL=false&serverTimezone=UTC
    username: root
    password: D@rp@n07
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 20000
      idle-timeout: 30000

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true



